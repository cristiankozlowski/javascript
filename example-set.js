const tags = new Set([]);

tags.add('js');
tags.add('javascript')
tags.add('javascript')
tags.add('javascript')
tags.add('javascript')
tags.add('javascript')

const result = Array.from(tags);

console.log(result);